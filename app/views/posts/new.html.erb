<div class="outerbox">

  <h3>New post</h3>
  <% if session[:user] == nil%>

      <%= "You are not signed in ! Click on login" %>   <%= link_to 'Login', '/login/login' %>

  <% else %>

      <%= render 'form' %>
  <% end %>
  <br/>
  <%= link_to 'BACK', posts_path, :class => 'btn' %>

</div>
